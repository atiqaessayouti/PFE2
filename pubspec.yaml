name: untitled1
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  http: ^0.13.6
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_spinkit: ^5.0.0

  # Firebase
  firebase_core: ^2.24.2
  cloud_firestore: ^4.0.0
  firebase_auth: ^4.18.1
  firebase_storage: ^11.0.0

  # Image picker
  image_picker: ^1.0.7

  # Pour les dates, prix, etc.
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - images/moto.png

